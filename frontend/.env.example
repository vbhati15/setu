# Copy to .env and fill in for production. Never commit .env.
# Left unset, the app falls back to "/api", which vite.config.js proxies to
# http://localhost:8001 in local dev.

VITE_API_URL=https://your-deployed-backend.example.com
